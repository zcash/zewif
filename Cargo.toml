[package]
name = "zewif"
version = "0.1.0"
edition = "2024"
license = "MIT or Apache 2.0"

[dependencies]
minicbor = { version = "1", features = ["derive", "std"] }

thiserror = "^2.0"
hex = "0.4.3"

rand = { version = "^0.9.2", optional = true }

[features]
default = []
with-context = []
test-dependencies = ["dep:rand"]

[dev-dependencies]
rand = "^0.9.2"
